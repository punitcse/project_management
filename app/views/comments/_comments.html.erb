<%= render "comments/new", comment: Comment.new, project: project %>

<hr>
<%= turbo_frame_tag "comments" do %>
    <% if comments.present? %>
    <%= render comments %>
    <% else %>
      No comments yet.
    <% end %>
<% end %>